machine:
  environment:
    COVERALLS_REPO_TOKEN: qJaHs6LtKVUC2SJcnDev0acWLtPuW1vXM
    PATH: $PATH:$HOME/node_modules/less/bin
dependencies:
  pre:
    - npm install less
test:
  override:
    - tox
